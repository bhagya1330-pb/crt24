#include <stdio.h>

int main()
{
 int *a,*b,*c;
 a=(int*)malloc(sizeof(int));
 b=(int*)malloc(sizeof(int));
 scanf("%d%d",a,b);
 c=(int*)malloc(sizeof(int));
 *c=*a+*b;
 free(a);
 free(b);
 printf("sum=%d",*c);
 free(c);
}
Output
4
5
sum=9

=== Code Execution Successful ===
